<!-- app/views/shared/_bottom_nav.html.erb -->
<nav class="lg:hidden fixed bottom-0 left-0 right-0 z-50 bg-white/90 dark:bg-stone-950/90 backdrop-blur-md border-t border-stone-200 dark:border-stone-800 transition-colors duration-300">
  <div class="flex justify-around py-2">
    <%= link_to root_path, class: "flex flex-col items-center py-2 px-4 #{current_page?(root_path) ? 'text-blue-500 dark:text-blue-400' : 'text-stone-500 dark:text-stone-400'} transition-colors" do %>
      <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 20 20">
        <path d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z"/>
      </svg>
      <span class="text-xs mt-1 font-medium">Início</span>
    <% end %>

    <%= link_to animals_path, class: "flex flex-col items-center py-2 px-4 #{current_page?(animals_path) ? 'text-blue-500 dark:text-blue-400' : 'text-stone-500 dark:text-stone-400'} transition-colors" do %>
      <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
        <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2z"/>
      </svg>
      <span class="text-xs mt-1 font-medium">Animais</span>
    <% end %>

    <%= link_to vaccinations_path, class: "flex flex-col items-center py-2 px-4 #{current_page?(vaccinations_path) ? 'text-blue-500 dark:text-blue-400' : 'text-stone-500 dark:text-stone-400'} transition-colors" do %>
      <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
        <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2z"/>
      </svg>
      <span class="text-xs mt-1 font-medium">Vacinas</span>
    <% end %>

    <% if current_user.can_manage_company? %>
      <%= link_to staff_users_path, class: "flex flex-col items-center py-2 px-4 #{current_page?(staff_users_path) ? 'text-blue-500 dark:text-blue-400' : 'text-stone-500 dark:text-stone-400'} transition-colors" do %>
        <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
          <path d="M16 4c0-1.11.89-2 2-2s2 .89 2 2-.89 2-2 2-2-.89-2-2zm4 18v-6h2.5l-2.54-7.63A3.01 3.01 0 0 0 16.98 7H15.2c-.39 0-.74.23-.9.59L12.8 11h-1.6l-1.5-3.41c-.16-.36-.51-.59-.9-.59H7.02a3.01 3.01 0 0 0-2.98 1.37L1.5 16H4v6h4v-6h2v6h4z"/>
        </svg>
        <span class="text-xs mt-1 font-medium">Usuários</span>
      <% end %>
    <% end %>
  </div>
</nav>

<script>
  function toggleSidebar() {
    const sidebar = document.getElementById('sidebar');
    sidebar.classList.toggle('-translate-x-full');
  }
  
  // Close sidebar when clicking outside on mobile
  document.addEventListener('click', function(event) {
    const sidebar = document.getElementById('sidebar');
    const toggle = event.target.closest('[onclick="toggleSidebar()"]');
    
    if (!toggle && !sidebar.contains(event.target) && window.innerWidth < 1024) {
      sidebar.classList.add('-translate-x-full');
    }
  });
</script>