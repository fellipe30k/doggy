<!-- app/views/animals/vaccination_card.html.erb -->
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Carteira de Vacinação - <%= @animal.name %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Arial', sans-serif;
      background: white;
      color: #333;
    }
    
    .card-container {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
    
    /* Portrait Format (300x600) */
    .card-portrait {
      width: 300px;
      height: 600px;
      background: white;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
      overflow: hidden;
      position: relative;
    }
    
    /* Landscape Format (600x300) */
    .card-landscape {
      width: 800px;
      height: 450px;
      background: white;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
      overflow: hidden;
      position: relative;
    }
    
    /* Header comum */
    .card-header {
      background: linear-gradient(135deg, #4f46e5, #7c3aed);
      color: white;
      padding: 15px;
      text-align: center;
      position: relative;
    }
    
    .card-header h1 {
      font-size: 16px;
      font-weight: bold;
      margin-bottom: 5px;
    }
    
    .card-header p {
      font-size: 12px;
      opacity: 0.9;
    }
    
    /* Portrait Layout */
    .portrait-content {
      padding: 20px;
      height: calc(600px - 80px);
      display: flex;
      flex-direction: column;
    }
    
    .portrait-top {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 20px;
    }
    
    .portrait-animal-photo {
      width: 80px;
      height: 80px;
      border-radius: 10px;
      object-fit: cover;
      border: 3px solid #4f46e5;
    }
    
    .portrait-qr-code {
      width: 70px;
      height: 70px;
      border: 2px solid #e5e7eb;
      border-radius: 8px;
      padding: 5px;
    }
    
    .portrait-animal-info {
      flex: 1;
      margin: 0 15px;
    }
    
    .portrait-animal-info h2 {
      font-size: 20px;
      color: #4f46e5;
      margin-bottom: 5px;
      font-weight: bold;
    }
    
    .portrait-animal-info .species {
      font-size: 14px;
      color: #6b7280;
      margin-bottom: 10px;
    }
    
    /* Landscape Layout */
    .landscape-content {
      padding: 15px;
      height: calc(300px - 60px);
      display: flex;
    }
    
    .landscape-left {
      width: 200px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: space-between;
      padding-right: 20px;
      border-right: 2px solid #e5e7eb;
    }
    
    .landscape-animal-photo {
      width: 100px;
      height: 100px;
      border-radius: 15px;
      object-fit: cover;
      border: 3px solid #4f46e5;
    }
    
    .landscape-qr-code {
      width: 80px;
      height: 80px;
      border: 2px solid #e5e7eb;
      border-radius: 8px;
      padding: 5px;
    }
    
    .landscape-right {
      flex: 1;
      padding-left: 20px;
      display: flex;
      flex-direction: column;
    }
    
    .landscape-animal-header h2 {
      font-size: 24px;
      color: #4f46e5;
      margin-bottom: 5px;
      font-weight: bold;
    }
    
    .landscape-animal-header .species {
      font-size: 16px;
      color: #6b7280;
      margin-bottom: 15px;
    }
    
    /* Dados comuns */
    .animal-details {
      background: #f9fafb;
      border-radius: 10px;
      padding: 15px;
      margin-bottom: 15px;
    }
    
    .detail-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 8px;
      font-size: 12px;
    }
    
    .detail-row:last-child {
      margin-bottom: 0;
    }
    
    .detail-label {
      font-weight: bold;
      color: #4b5563;
    }
    
    .detail-value {
      color: #1f2937;
    }
    
    .owner-info {
      background: #eff6ff;
      border-radius: 10px;
      padding: 15px;
      margin-bottom: 15px;
      border-left: 4px solid #3b82f6;
    }
    
    .owner-title {
      font-size: 14px;
      font-weight: bold;
      color: #1e40af;
      margin-bottom: 10px;
    }
    
    .vaccinations-summary {
      background: #f0fdf4;
      border-radius: 10px;
      padding: 12px;
      border-left: 4px solid #22c55e;
    }
    
    .vaccination-title {
      font-size: 12px;
      font-weight: bold;
      color: #166534;
      margin-bottom: 8px;
    }
    
    .vaccination-item {
      font-size: 10px;
      color: #15803d;
      margin-bottom: 3px;
      display: flex;
      justify-content: space-between;
    }
    
    .no-photo-placeholder {
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, #e5e7eb, #d1d5db);
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 10px;
      color: #6b7280;
      font-size: 12px;
      text-align: center;
    }
    
    .card-footer {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      background: #f8fafc;
      padding: 8px;
      text-align: center;
      font-size: 10px;
      color: #6b7280;
      border-top: 1px solid #e5e7eb;
    }
    
    /* Print styles */
    @media print {
      .card-container {
        background: white;
        padding: 0;
        min-height: auto;
      }
      
      .card-portrait, .card-landscape {
        box-shadow: none;
        margin: 0;
      }
    }
    
    /* Controls */
    .controls {
      position: fixed;
      top: 20px;
      right: 20px;
      z-index: 1000;
      display: flex;
      gap: 10px;
    }
    
    .btn {
      padding: 8px 16px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 500;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }
    
    .btn-primary {
      background: #4f46e5;
      color: white;
    }
    
    .btn-secondary {
      background: #6b7280;
      color: white;
    }
    
    .btn:hover {
      opacity: 0.9;
    }
    
    @media print {
      .controls {
        display: none;
      }
    }
  </style>
</head>
<body>
  <!-- Controles -->
  <div class="controls">
    <button onclick="window.print()" class="btn btn-primary">
      🖨️ Imprimir
    </button>
    <a href="<%= vaccination_card_animal_path(@animal, format: @format == 'portrait' ? 'landscape' : 'portrait') %>" class="btn btn-secondary">
      🔄 Alternar Formato
    </a>
    <a href="<%= animal_path(@animal) %>" class="btn btn-secondary">
      ← Voltar
    </a>
  </div>

  <div class="card-container">
    <div class="<%= @format == 'portrait' ? 'card-portrait' : 'card-landscape' %>">
      <!-- Header -->
      <div class="card-header">
        <h1>🐕 CARTEIRA DE VACINAÇÃO</h1>
        <p>Sistema Doggy - <%= @animal.company.name %></p>
      </div>

      <% if @format == 'portrait' %>
        <!-- Portrait Layout (300x600) -->
        <div class="portrait-content">
          <div class="portrait-top">
            <!-- Foto do Animal -->
            <div>
              <% if @animal.photo.attached? %>
                <%= image_tag @animal.photo, class: "portrait-animal-photo", alt: @animal.name %>
              <% else %>
                <div class="portrait-animal-photo">
                  <div class="no-photo-placeholder">
                    🐕<br>Sem<br>Foto
                  </div>
                </div>
              <% end %>
            </div>

            <!-- Informações do Animal -->
            <div class="portrait-animal-info">
              <h2><%= @animal.name %></h2>
              <div class="species"><%= @animal.species %> • <%= @animal.breed %></div>
            </div>

            <!-- QR Code -->
            <div>
              <img src="<%= @qr_code_data_url %>" class="portrait-qr-code" alt="QR Code">
            </div>
          </div>

          <!-- Dados do Animal -->
          <div class="animal-details">
            <div class="detail-row">
              <span class="detail-label">Idade:</span>
              <span class="detail-value"><%= @animal.age_in_years %> anos</span>
            </div>
            <div class="detail-row">
              <span class="detail-label">Peso:</span>
              <span class="detail-value"><%= number_with_precision(@animal.weight, precision: 1) %> kg</span>
            </div>
            <div class="detail-row">
              <span class="detail-label">Cor:</span>
              <span class="detail-value"><%= @animal.color || 'Não informada' %></span>
            </div>
            <div class="detail-row">
              <span class="detail-label">Nascimento:</span>
              <span class="detail-value"><%= @animal.birth_date.strftime("%d/%m/%Y") %></span>
            </div>
            <% if @animal.microchip.present? %>
              <div class="detail-row">
                <span class="detail-label">Microchip:</span>
                <span class="detail-value"><%= @animal.microchip %></span>
              </div>
            <% end %>
          </div>

          <!-- Dados do Proprietário -->
          <div class="owner-info">
            <div class="owner-title">👤 PROPRIETÁRIO</div>
            <div class="detail-row">
              <span class="detail-label">Nome:</span>
              <span class="detail-value"><%= @animal.owner_name || 'Não informado' %></span>
            </div>
            <% if @animal.owner_phone.present? %>
              <div class="detail-row">
                <span class="detail-label">Telefone:</span>
                <span class="detail-value"><%= @animal.owner_phone %></span>
              </div>
            <% end %>
            <% if @animal.owner_email.present? %>
              <div class="detail-row">
                <span class="detail-label">E-mail:</span>
                <span class="detail-value"><%= @animal.owner_email %></span>
              </div>
            <% end %>
          </div>

          <!-- Resumo das Vacinações -->
          <div class="vaccinations-summary">
            <div class="vaccination-title">💉 ÚLTIMAS VACINAÇÕES</div>
            <% if @vaccinations.any? %>
              <% @vaccinations.first(5).each do |vaccination| %>
                <div class="vaccination-item">
                  <span><%= vaccination.vaccine_name %></span>
                  <span><%= vaccination.application_date.strftime("%m/%Y") %></span>
                </div>
              <% end %>
              <% if @vaccinations.count > 5 %>
                <div class="vaccination-item">
                  <span>+ <%= @vaccinations.count - 5 %> outras vacinas</span>
                  <span>...</span>
                </div>
              <% end %>
            <% else %>
              <div class="vaccination-item">
                <span>Nenhuma vacinação registrada</span>
                <span>-</span>
              </div>
            <% end %>
          </div>
        </div>

      <% else %>
        <!-- Landscape Layout (600x300) -->
        <div class="landscape-content">
          <div class="landscape-left">
            <!-- Foto do Animal -->
            <div>
              <% if @animal.photo.attached? %>
                <%= image_tag @animal.photo, class: "landscape-animal-photo", alt: @animal.name %>
              <% else %>
                <div class="landscape-animal-photo">
                  <div class="no-photo-placeholder">
                    🐕<br>Sem Foto
                  </div>
                </div>
              <% end %>
            </div>

            <!-- QR Code -->
            <div>
              <img src="<%= @qr_code_data_url %>" class="landscape-qr-code" alt="QR Code">
            </div>
          </div>

          <div class="landscape-right">
            <!-- Header do Animal -->
            <div class="landscape-animal-header">
              <h2><%= @animal.name %></h2>
              <div class="species"><%= @animal.species %> • <%= @animal.breed %></div>
            </div>

            <div style="display: flex; gap: 15px; flex: 1;">
              <!-- Dados do Animal -->
              <div style="flex: 1;">
                <div class="animal-details" style="margin-bottom: 10px; padding: 10px;">
                  <div class="detail-row">
                    <span class="detail-label">Idade:</span>
                    <span class="detail-value"><%= @animal.age_in_years %> anos</span>
                  </div>
                  <div class="detail-row">
                    <span class="detail-label">Peso:</span>
                    <span class="detail-value"><%= number_with_precision(@animal.weight, precision: 1) %> kg</span>
                  </div>
                  <div class="detail-row">
                    <span class="detail-label">Cor:</span>
                    <span class="detail-value"><%= @animal.color || 'Não informada' %></span>
                  </div>
                  <div class="detail-row">
                    <span class="detail-label">Nascimento:</span>
                    <span class="detail-value"><%= @animal.birth_date.strftime("%d/%m/%Y") %></span>
                  </div>
                  <% if @animal.microchip.present? %>
                    <div class="detail-row">
                      <span class="detail-label">Microchip:</span>
                      <span class="detail-value" style="font-size: 10px;"><%= @animal.microchip %></span>
                    </div>
                  <% end %>
                </div>

                <!-- Dados do Proprietário -->
                <div class="owner-info" style="margin-bottom: 0; padding: 10px;">
                  <div class="owner-title">👤 PROPRIETÁRIO</div>
                  <div class="detail-row">
                    <span class="detail-label">Nome:</span>
                    <span class="detail-value"><%= @animal.owner_name || 'Não informado' %></span>
                  </div>
                  <% if @animal.owner_phone.present? %>
                    <div class="detail-row">
                      <span class="detail-label">Telefone:</span>
                      <span class="detail-value"><%= @animal.owner_phone %></span>
                    </div>
                  <% end %>
                  <% if @animal.owner_email.present? %>
                    <div class="detail-row">
                      <span class="detail-label">E-mail:</span>
                      <span class="detail-value" style="font-size: 10px;"><%= @animal.owner_email %></span>
                    </div>
                  <% end %>
                </div>
              </div>

              <!-- Vacinações -->
              <div style="flex: 1;">
                <div class="vaccinations-summary" style="padding: 10px; height: 100%;">
                  <div class="vaccination-title">💉 ÚLTIMAS VACINAÇÕES</div>
                  <% if @vaccinations.any? %>
                    <% @vaccinations.first(8).each do |vaccination| %>
                      <div class="vaccination-item">
                        <span><%= vaccination.vaccine_name %></span>
                        <span><%= vaccination.application_date.strftime("%m/%Y") %></span>
                      </div>
                    <% end %>
                    <% if @vaccinations.count > 8 %>
                      <div class="vaccination-item">
                        <span>+ <%= @vaccinations.count - 8 %> outras</span>
                        <span>...</span>
                      </div>
                    <% end %>
                  <% else %>
                    <div class="vaccination-item">
                      <span>Nenhuma vacinação registrada</span>
                      <span>-</span>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% end %>

      <!-- Footer -->
      <div class="card-footer">
        Emitido em <%= Date.current.strftime("%d/%m/%Y") %> • 
        Escaneie o QR Code para ver o histórico completo • 
        Sistema Doggy
      </div>
    </div>
  </div>
</body>
</html>